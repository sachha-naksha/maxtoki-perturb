"""PyTorch / BioNeMo pipeline for zero-shot gene-inhibition scoring on the
MaxToki temporal head. Sibling to the MLX package; no MLX dependencies.

Run as a module from the repo root:
    python -m scripts.torch_pipeline.run_inhibit_temporal_mse --help
"""
